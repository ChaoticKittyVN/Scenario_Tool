# ChaoticKittyScenario 演出工具版本日志

本文档记录正式版本的主要功能变化。日期以版本完成或发布节点为准。

## 0.10.0 - 2026-08-07（重要更新）

### 参数数据与项目架构

- **多项目参数维护** - 支持通过 `processing.multi_project_mode` 和 `projects` 为同一套工具配置多个项目或篇章，并按演出表文件名合并专用参数。
- **单项目参数选择** - 单项目配置一个项目时，优先读取 `param_data_<engine>_<project>.xlsx`，并兼容旧的 `param_data_<project>.xlsx`。
- **可选引擎架构** - 新增 `engines.enabled` 配置，只加载项目实际存在且启用的引擎；删除未使用的引擎目录后不再因缺少对应配置而启动失败。
- **参数更新模块化** - 将庞大的 `update_param.py` 拆分到 `core/param_update/`，保留直接执行时生成映射并同步演出表格参数表的原有流程。
- **参数更新独立入口** - 新增基础映射、参数表同步、普通差分映射和 Agent 差分文档的独立 CLI 与 GUI 操作。
- **参数表写入优化** - 参数表内容完全一致时跳过保存，缺少参数表时自动创建。

### Agent 差分文档

- **自定义差分描述列** - `variant_data.xlsx` 可以增加情绪、序号、适用情绪、眼、嘴、眉毛等不固定的描述列。
- **组合式差分参数** - 支持使用配置模板将情绪、序号等列组合为 `ScenarioParam`。
- **结构化 Agent 导出** - 新增 `variant_agent_data.json`，保留自定义属性并生成分组选择索引和适用情绪索引，方便 Agent 理解与选择差分。
- **双重差分输出** - 普通 `variant_mappings.py` 与 Agent 专用 JSON 相互独立，不改变既有脚本生成流程。

### GUI 与工具工作流

- **动态工具箱** - GUI 自动扫描 `tools/` 中使用 argparse 的脚本并生成参数控件，不需要为每个工具写死页面逻辑。
- **参数界面优化** - 改善参数类型、必填项、可选值、文件选择和执行状态展示，并修复复选框在部分主题中不可见的问题。
- **本机设置分离** - 工具执行 Python、窗口尺寸等本机 GUI 设置与项目 `config.yaml` 分开保存，项目配置保存时保留未知字段。
- **批量工作流执行器** - `tools/run_workflow.py` 支持预览、正式执行、步骤筛选、变量覆盖、Python 选择和 JSON 报告。
- **通用表格变更** - `tools/smart_fill.py` 与表格变换核心支持声明式规则，同时保留专用工具处理复杂业务。

### 字数统计

- **演出表字数统计** - 新增 `tools/count_words.py`，按与正式生成一致的 END 和 Ignore 规则统计文本。
- **多层明细** - 支持按文件、工作表和角色输出统计结果，并可导出 JSON 报告。
- **角色筛选** - 支持只统计或排除指定角色，并可按需纳入特殊名称。
- **统计修复** - 修复角色与文本行对应、特殊名称筛选和表尾范围处理问题。

### 稳定性与文档

- **缺失值与异常处理** - 改进缺失参数、生成器失败和可选引擎加载错误的处理。
- **测试更新** - 修复旧测试对调整后入口、配置模型和工具名称的引用；发布前完整测试结果为 `392 passed`。
- **用户文档更新** - 补充当前配置、参数映射、Agent 差分、多项目、工具箱和批量工作流说明，并修正工具迁移到 `tools/` 后的命令路径。

### 已知后续项

- Utage 的部分专用处理仍位于通用核心和 Excel writer 中，计划在后续版本下沉到 Utage 引擎包。
- `run_tool.py` 尚未作为正式入口；批量任务使用 `tools/run_workflow.py`。
- 当前轻量 GUI 启动器依赖开发者本机 Python 环境，不作为 0.10.0 发布内容。

## 0.9.2 - 2026-08-06

- **参数更新预览** - `update_param.py` 支持指定工作簿和安全预览，便于在写入前确认操作范围。
- **幂等参数表更新** - 内容未变化时跳过保存，并精简多文件更新日志。
- **声明式表格变换** - 新增通用表格变换流程，为智能填充和批量人工劳动提供基础能力。
- **动态 GUI 工具页** - 将 `tools/` 脚本动态整合到 GUI，可在窗口中填写参数、执行、查看输出和停止进程。
- **生成器调整** - 更新 Ren'Py 与 Naninovel 的部分语句生成器和参数处理。
- **文档与配置保存** - 更新多引擎说明，并改善 GUI 配置保存和工具入口。

## 0.9.1 - 2026-08-05

- **背景与等待生成器增强** - 为背景和暂停等待逻辑增加新属性与处理方式。
- **自动阻塞转场** - 新增 `AutoBlockTransition` 枚举，用于控制自动阻塞转场行为。
- **镜头处理能力** - 增加 Camera 参数处理，并扩展角色移动和镜头命令。
- **角色动画增强** - 支持动画重复配置，改进角色位置和移动计算。
- **Naninovel 引擎迭代** - 更新背景、镜头、角色移动、转场效果和文本格式逻辑。
- **Ren'Py 引擎迭代** - 增加 `TransScene`、`TransSub` 等参数并改进暂停和转场处理。
- **台本导出扩展** - 改善配音台本数据提取，新增脚本审查与翻译表导出工具。
- **引擎解耦** - 精简 Excel 文件处理流程，减少不必要的固定引擎导入。

## 0.9.0 - 2026-03-03 至 2026-03-16（重要更新）

- **语音列支持** - 在 `ColumnName` 枚举中增加语音列，并实现语音文件相关的对话脚本处理。
- **配置和引擎处理器增强** - 支持生成器类别，并增加语音配音测试场景脚本导出功能。
- **参数验证重构** - 将 `ParamTranslator` 移至参数处理模块，新增 `ParamValidator` 负责参数验证。
- **等待生成器** - 新增 `WaitGenerator` 用于生成等待命令，并向 `SpecialName` 枚举增加相应命令。
- **独占生成器处理** - 在 `EngineProcessor` 中实现独占生成器流程，并为 Utage 更新 `DirectCommandGenerator` 的文本参数处理。
- **生成器增强** - 更新音频、背景、角色文本、直接命令、宏和转场生成器，增加验证类型和新的处理逻辑。
- **变量名修正** - 统一修正代码库中的拼写错误 `varient` 为 `variant`。
